{{#if  isViewingDepartments }}
<div>
    <h4>{{facultyName}}'s Departments</h4>
    <table>
        <tr>
            <th>
                Department
            </th>
            <th> 
                Admin 
            </th>
            <th>
                Delete
            </th>
        </tr>
        {{#each departments as |departmentChoice|}}
        <tr>
            <td>
                {{departmentChoice.name}}
            </td>
            <td>
                {{view-programadministration ID=departmentChoice.id}}
            </td>
            <td>
                {{delete-department ID=departmentChoice.id}}
            </td>
        </tr>
        {{/each}}
    </table>


<!--     {{#each departments as |departmentChoice|}}
    <div class="row" align="left">
        <div class="large-1 columns">
            {{delete-department ID=departmentChoice.id}}
        </div>
        <div class="large-11 columns">
            <h5>{{departmentChoice.name}}</h5>
        </div>
    </div>
    <div class="row" align="left">
        <div class="large-12 columns">
            {{view-programadministration ID=departmentChoice.id}}
        </div>
    </div>
    {{/each}} -->
    <div align="center">
        <a href="#" class="button primary tiny other" {{action 'doneViewingDepartments'}}>Hide Departments</a>
    </div>
</div>
{{else}}
<div class="large-2 columns">
    <a href="#" class="button primary tiny other" {{action 'viewDepartments'}}>View Departments</a>
</div>
{{/if}}